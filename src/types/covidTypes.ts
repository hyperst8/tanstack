export interface CovidData {
  name: string; // Country name redefined
  country: string; // Country from API
  cases: number; // Total cases
  deaths: number; // Total deaths
  recovered: number; // Total recovered
}
